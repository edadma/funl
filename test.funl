def
  filter(p, [])           =  []
  filter(p, x:xs) | p(x)  =  x:filter(p, xs)
  filter(p, _:xs)         =  filter(p, xs)

filter((>3), [1, 2, 3, 4, 5])

def f([]) = write("empty")
def f(h:t) = write(h, t)

;;f([])
